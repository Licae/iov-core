# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# EXECUTION_MODE: Optional. Supported values:
# - python: python adapter
# - shell/script: shell adapter
# - simulate: built-in fake executor, only for local debugging
EXECUTION_MODE="python"

# EXECUTION_SCRIPT: Optional when EXECUTION_MODE=script.
# Supported placeholders: {{taskId}} {{testCaseId}} {{suiteId}} {{assetId}} {{title}} {{protocol}}
# Example:
# EXECUTION_SCRIPT="bash scripts/run_case.sh {{testCaseId}}"
EXECUTION_SCRIPT=""

# PYTHON_EXECUTABLE: Optional when using the python adapter.
PYTHON_EXECUTABLE="python3"

# PYTHON_SECURITY_RUNNER: Optional when using the python adapter.
# Default points to scripts/security_runner.py in the repo root.
PYTHON_SECURITY_RUNNER=""

# DB_PATH: Optional SQLite database path (repo-root relative or absolute).
# Default behavior:
# 1) if ./v2x_testing.db exists, continue using it (legacy compatibility)
# 2) otherwise use ./runtime-data/v2x_testing.db
DB_PATH="runtime-data/v2x_testing.db"

# Runtime artifact storage (project-root relative by default).
# Example: runtime-artifacts
RUNTIME_ARTIFACTS_DIR="runtime-artifacts"

# Retention policy (hours):
# - Passed tasks: default 0 (clean immediately)
# - Failed/Blocked tasks: default 24
# - Cancelled tasks: default 2
ARTIFACT_RETENTION_HOURS_PASS="0"
ARTIFACT_RETENTION_HOURS_FAIL="24"
ARTIFACT_RETENTION_HOURS_CANCEL="2"

# Startup cleanup and quota guard.
ARTIFACT_CLEAN_ON_START="true"
ARTIFACT_MAX_SIZE_MB="2048"
ARTIFACT_MAX_DIRS="5000"
